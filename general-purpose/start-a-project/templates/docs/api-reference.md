# API Reference

> Last updated: __DATE__  
> Status: empty — fill in as the system takes shape.

The API surface this project exposes (or consumes). Keep generated and authored sections clearly separated.

## Authentication

_TBD — schemes supported (API key, OAuth2, mTLS, JWT). Where credentials come from. Token lifetime and rotation._

## Base URLs

| Environment | URL |
|-------------|-----|
| _TBD_ | _TBD_ |

## Endpoints

_TBD — one block per endpoint with method, path, request schema, response schema, error codes. If generated from an OpenAPI/Protobuf spec, link to the spec and note the regen command._

### `METHOD /path`

- **Purpose:** _TBD_
- **Request:** _TBD_
- **Response:** _TBD_
- **Errors:** _TBD_

## Schemas

_TBD — shared request/response shapes. Reference once, link from each endpoint._

## Errors

_TBD — error code taxonomy: when each is returned and what the caller should do._

## Versioning

_TBD — how the API is versioned (URL, header, none) and the deprecation policy._

## Rate Limits

_TBD — per-key, per-IP, per-endpoint limits. Headers returned. Backoff guidance._

## Webhooks / Callbacks

_TBD — outbound calls this system makes, signing, retries._
