# Source Map

> Last updated: __DATE__  
> Status: empty — fill in as the system takes shape.

Where to find things in this repository.

## Repository Structure

_TBD — top-level tree with a one-line purpose per directory. Generate with `tree -L 2 -d` and annotate._

```
.
├── ...
└── ...
```

## Key Source Files

_TBD — the 10–20 files a new contributor must read first. For each: path, purpose, who calls it, what it owns._

## Dependencies

_TBD — runtime + dev dependencies, where they are pinned, and the policy for upgrades._

## Platform Requirements

_TBD — OS, language version, system libraries, hardware (GPU, RAM)._

## Build Artifacts

_TBD — what the build emits and where it lands (`dist/`, `target/`, image registry, etc.)._

## Generated Code

_TBD — paths under version control that are produced by tooling, plus the command that regenerates them._
